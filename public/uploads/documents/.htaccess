# Deny direct access to all files
<Files *>
    Order Deny,Allow
    Deny from all
</Files>

# Allow access only through PHP scripts
<FilesMatch "\.(pdf|doc|docx)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>